#Input folder - the application is reading all the files from this directory 
geocoding.input.folder=target/input

#After a file is processed it's placed in this directory
geocoding.output.folder=target/output

#Url to the google api - in tests we start a rest simulator for this - use localhost
geocoding.url=http://nbtevo:8080/json

#The key for the API - you need to register for this key
geocoding.API_KEY=TEST